function openMenu() {
  document.body.classList += " menu--open";
}

function closeMenu() {
  document.body.classList.remove("menu--open");
}

const adv = (arr) => {
  return starStr;
};

console.log(adv(5));
